{
  "name": "tickjs",
  "version": "0.1.1",
  "description": "a tiny time handler",
  "main": "dist/tick.js",
  "keywords": [
    "time",
    "date",
    "tick"
  ],
  "scripts": {
    "dev": "NODE_ENV=dev rollup -c build/rollup.config.dev.js -w",
    "build": "NODE_ENV=prod rollup -c build/rollup.config.build.js",
    "docs": "jsduck dist/tick.js --output docs",
    "test": "NODE_ENV=test mocha -w -b"
  },
  "author": "daben",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-plugin-external-helpers": "^6.22.0",
    "babel-preset-latest": "^6.24.1",
    "mocha": "^5.1.1",
    "rollup-plugin-babel": "^3.0.4",
    "rollup-plugin-uglify": "^3.0.0",
    "should": "^13.2.1"
  }
}
